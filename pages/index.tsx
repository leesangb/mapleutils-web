import { Button } from '@mui/material';
import type { NextPage } from 'next';

const Home: NextPage = () => {
    return (
        <>
            <Button>Hello World</Button>
        </>
    );
};

export default Home;
